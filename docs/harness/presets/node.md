# Node / npm 프리셋

## globs (`.cursor/rules/`)

| 규칙 | globs |
|------|-------|
| backend | `server/**`, `api/**`, `packages/api/**` |
| frontend | `client/**`, `apps/web/**`, `src/**` |

## run-eval

```powershell
# scripts/run-eval.ps1 상단
# npm test
```

## sample

`sample/` TB-101 구조를 `server/` + `client/`로 복사.

## MCP (선택)

GitHub MCP · Postman MCP — `mcp-setup.md`
