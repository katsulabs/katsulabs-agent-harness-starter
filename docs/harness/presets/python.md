# Python / pytest 프리셋

## globs

| 규칙 | globs |
|------|-------|
| backend | `backend/**`, `api/**`, `src/**` |
| frontend | `frontend/**`, `client/**` |

## run-eval

```powershell
# pyproject.toml 있으면 자동: pytest
```

## contract

`contracts/` 또는 `**/openapi.*` + Pydantic DTO
