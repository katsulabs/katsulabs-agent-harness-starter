name: harness-gate

on:
  pull_request:
    branches: [main]
  push:
    branches: [main, 'feature/**', 'cursor/**']

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Harness validate
        shell: pwsh
        run: ./scripts/validate-harness.ps1

  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Project eval
        shell: pwsh
        run: ./scripts/run-eval.ps1
